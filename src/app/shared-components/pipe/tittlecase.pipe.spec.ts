import { TittlecasePipe } from './tittlecase.pipe';

describe('TittlecasePipe', () => {
  it('create an instance', () => {
    const pipe = new TittlecasePipe();
    expect(pipe).toBeTruthy();
  });
});
